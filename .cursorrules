# Prophet 项目规则

## AI 编程助手强制行为规范（必须遵守）

### 语言与沟通
- **必须使用中文（简体）回复**，除非用户明确要求英文。
- **遇到不确定/缺少信息时先问清楚再改**，不要凭空假设业务规则或数据库结构。

### 文档与文件生成（高优先级强约束）
- **未经用户明确允许，禁止创建任何 `.md` 文件**（包括 README、指南、设计文档、变更日志等）。
- 允许：**读取/引用/修改已存在的 `.md` 文件**。
- 如果确实需要新建 `.md`：先说明“要创建什么、解决什么问题、放在哪”，并等待用户明确同意后再创建。

### 调试与日志（高优先级强约束）
- 终端输出日志一律使用 `Console.WriteLine()`。
- **禁止**使用 `System.Diagnostics.Debug.WriteLine()` 作为运行时日志输出。

### 数据库访问（高优先级强约束）
- **禁止绕过 `DBHelper` 直接操作数据库**（包括直接拼接/执行 SQL）。
- 结构变更相关 SQL：必须放入 `SQL Scripts/` 目录，并遵循现有脚本风格。
- Python 访问 MySQL：必须使用 `mysql.py` 模块。

### 交易/信号业务规则（高优先级强约束）
- 交易信号必须**完全基于指标结果与数据分析**。
- **禁止时间过滤**（不要基于时间段/胜率历史等限制交易）。
- **禁止基于历史亏损限制订单量**。

### 重构规则（高优先级强约束）
- 除非用户明确要求，**重构不考虑向后兼容**：该删就删，不保留旧接口/旧实现，不维护两套逻辑。

### 核心服务类修改的安全流程（必须）
- 修改 `DSLValidator` / `IntelliSenseService` / `CodeEditorHelper` 等核心服务前：
  - 先定位调用链与配置文件依赖（如 `.xshd` / `.yaml` / `.json`）
  - 明确影响范围（编辑器高亮/提示/验证/回测等）
  - 再实施修改，避免引入行为漂移

## 文档生成规则

### ❌ 禁止未经允许生成 Markdown 文档
- **严格禁止** AI 编程助手在未经用户明确允许的情况下生成任何 `.md` 文档
- 包括但不限于：README.md、使用指南、API文档、更新日志、技术说明等
- 如果需要创建文档，必须：
  1. 先向用户说明需要创建的文档及其目的
  2. 等待用户明确同意
  3. 获得同意后才能创建

### ✅ 允许的文档操作
- **修改现有文档**：可以编辑、更新已存在的 `.md` 文件
- **阅读文档**：可以读取和引用项目中的文档
- **建议文档**：可以建议应该创建什么文档，但不能直接创建

### 示例

**❌ 错误做法**：
```
"我已经为你创建了 API使用指南.md..."
"已生成 更新说明.md 文档..."
```

**✅ 正确做法**：
```
"我建议创建一个API使用指南文档，包含以下内容：...
是否需要我创建这个文档？"
```

---

## 项目概述

**Prophet** 是一个适用于加密货币的量化交易平台，Prophet DSL是平台支持的用于编写量化交易策略的特定领域的编程语言。

### 核心功能
- 使用自定义 DSL 语言编写交易策略
- 可视化策略编辑器（语法高亮、智能提示）
- 策略回测与优化（使用 Optuna）
- 参数管理与持久化

---

## 技术栈

### 前端技术
- **UI框架**: Avalonia (跨平台 .NET UI 框架)
- **代码编辑器**: AvaloniaEdit
- **语法高亮**: .xshd 格式配置文件 (`StrategyDSL.xshd`)
- **智能提示**: YAML 配置 (`IntelliSense.yaml`)
- **补全规则**: JSON 配置 (`CompletionRules.json`)

### 后端技术
- **语言**: C# (.NET)
- **数据库**: SQLite
- **数据库访问**: DBHelper 工具类
- **优化框架**: Optuna

### 关键服务类
- `DSLValidator`: DSL 语法验证
- `CodeEditorHelper`: 代码编辑器辅助功能
- `IntelliSenseService`: 智能提示服务

---

## 项目架构

```
Prophet/
├── Prophet.Client/         # Avalonia UI 客户端
│   ├── Services/           # 核心服务（验证器、编辑器、智能提示等）
│   ├── Resources/          # 资源文件（语法高亮、智能提示配置等）
│   └── ...
├── Prophet.Core/           # 核心策略引擎
├── doc/                    # 项目文档
│   └── Prophet DSL 规范/   # DSL 语言规范文档
├── optuna_core.db          # 主数据库（持久化数据）
└── trades.db               # 临时交易数据（每次回测后清空）
```

---

## 代码规范

### 命名规范
- ✅ 使用有意义的变量名
- ❌ **避免使用冗余后缀**: 如 `_d`、`_data` 等
- ✅ 遵循 C# 命名约定（PascalCase for 类/方法，camelCase for 局部变量）

### 代码质量
- 保持代码风格一致
- 添加必要的注释（特别是复杂逻辑）
- 遵循项目现有的命名规范
- 适当使用空行分隔逻辑块

### 代码修改
- ✅ 可以修改代码文件（.cs, .cpp, .py, .js等）
- ✅ 可以修改配置文件（.yaml, .json, .xml等）
- ✅ 可以创建新的代码文件
- ⚠️ 修改核心服务类时要格外小心

### 调试与日志
- **终端输出日志**: 使用 `Console.WriteLine()` 函数进行终端输出
- **禁止使用**: `System.Diagnostics.Debug.WriteLine()` 用于终端日志输出
- **原因**: 项目统一使用 Console 输出,便于在运行时查看日志

---

## 数据库规则

### 数据库
- ✅ 客户端使用 SQLite 数据库
- ✅ 后端使用 MySQL 数据库
- ✅ Python脚本访问MySQL数据库必须使用mysql.py模块
- ✅ SQL Scripts目录下的prophet.sql脚本是MySQL数据库的结构，可以通过它了解数据库表结构
- ✅ 生成的SQL脚本必须存放在SQL Scripts目录下

---

## 业务逻辑规则

### 交易信号规则
- ✅ **仅依据指标**: 交易信号必须完全基于指标结果和数据分析
- ❌ **禁止时间过滤**: 不要根据时间段、胜率历史等限制交易
- ❌ **禁止基于亏损限制**: 不要根据历史亏损限制订单量
- **原则**: 让数据和指标说话，不要人为添加时间或历史表现的限制

### DSL 策略开发
- 遵循 `doc/Prophet DSL 规范/Prophet DSL 函数.md` 中的规范
- 使用 `DSLValidator` 进行语法验证
- 参考 `StrategyDSL.xshd` 了解语法高亮规则

---

## 文件约定

### 资源文件
- **StrategyDSL.xshd**: DSL 语法高亮定义（XML 格式）
- **IntelliSense.yaml**: 智能提示配置（函数、变量、常量等）
- **CompletionRules.json**: 代码补全规则
- **IconPaths.axaml**: 图标路径定义

### 修改资源文件时注意
- YAML 文件注意缩进（使用空格，不要用制表符）
- JSON 文件确保格式正确
- XSHD 文件遵循 XML 规范

---

## 版本控制

### Git 规则
- ❌ 不要修改 git 配置
- ❌ 不要执行破坏性的 git 操作（force push, hard reset等）
- ❌ 不要跳过 git hooks
- ✅ 只在用户明确要求时才提交代码

---

## 代码重构规则

### ❌ 不考虑向后兼容
- **严格禁止**在代码重构时考虑向后兼容性
- 除非用户**明确要求**，否则所有重构都应该彻底、干净，删除旧代码
- 不要保留废弃的接口、方法或数据结构
- 不要为了兼容性而维护两套代码

### ✅ 允许的情况
- 用户明确要求保持向后兼容
- 用户明确要求保留某些旧接口

### 示例

**❌ 错误做法**：
```
// 保留旧方法以维持兼容性
void oldMethod() { ... }
void newMethod() { ... }
```

**✅ 正确做法**：
```
// 直接使用新方法，删除旧方法
void newMethod() { ... }
```

---

## 禁止事项清单

### 绝对禁止
1. ❌ 未经允许创建 .md 文档
2. ❌ 使用 `Debug.WriteLine()` 替代 `Console.WriteLine()`
3. ❌ 绕过 DBHelper 直接操作数据库
4. ❌ 在交易逻辑中添加时间过滤或亏损限制
5. ❌ 使用冗余变量名后缀（如 `_d`）
6. ❌ 使用 CLI 参数替代静态配置变量
7. ❌ 在代码重构时考虑向后兼容（除非用户明确要求）

### 需谨慎
1. ⚠️ 修改核心服务类（DSLValidator, IntelliSenseService 等）
2. ⚠️ 修改资源配置文件（确保格式正确）
3. ⚠️ 数据库结构变更（可能影响现有数据）

---

**版本**: 2.0  
**创建日期**: 2025-11-09  
**最后更新**: 2025-11-10

