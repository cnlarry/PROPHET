# 回测流程重构 - 待完成工作

## 已完成 ✅

1. **BacktestConfig.cs**
   - 已添加 `SignalSamplingInterval` (信号采样频率)
   - 已添加 `UseApiDataSource` (使用API数据源)

2. **ApiDataFeed.cs**
   - 已创建完整实现
   - 支持从API加载K线、恐慌指数、资金费率

3. **BacktestEngine.cs**
   - 已重构滑动窗口逻辑
   - 已添加 `ParseIntervalToMinutes()` 方法
   - 已修改 `PrepareDataAsync()` 统一加载所有数据

## 待完成 ❌

### 4. DslStrategySignalGenerator.cs

#### 修改点 A: 字段修改
删除:
```csharp
private readonly TimeSeriesApiClient _timeSeriesClient;
```

添加:
```csharp
private List<FearGreedData>? _fearGreedData;
private List<FundingRateData>? _fundingRateData;

public void SetTimeSeriesData(List<FearGreedData> fearGreedData, List<FundingRateData> fundingRateData)
{
    _fearGreedData = fearGreedData;
    _fundingRateData = fundingRateData;
}
```

#### 修改点 B: 移除构造函数中的TimeSeriesApiClient
删除构造函数或改为空构造函数

#### 修改点 C: 使用实际DSL而不是测试DSL
将:
```csharp
string simpleDsl = @"// 新策略...";
_engineHandle = ProphetCoreEngine.Prophet_CreateEngine(simpleDsl, paramsJson);
```

改为:
```csharp
_engineHandle = ProphetCoreEngine.Prophet_CreateEngine(_dslCode, paramsJson);
```

#### 修改点 D: 注入时间序列数据
在 `_isInitialized = true;` 之后添加注入逻辑

#### 修改点 E: 删除LoadTimeSeriesDataAsync方法

#### 修改点 F: 添加注入方法
添加 `InjectFearGreedData()` 和 `InjectFundingRateData()` 方法

### 5. BacktestViewModel.cs

使用 `ApiDataFeed` 替代 `HistoricalDataFeed`

### 6. BacktestView.axaml

添加信号采样频率选择UI控件

## 核心改变

1. **数据源**: 从本地KlineCache → API统一加载
2. **遍历方式**: 逐根K线 → 按采样频率滑动窗口  
3. **时间序列**: Initialize时加载 → PrepareData阶段统一加载

## 优势

- ✅ 不依赖本地缓存
- ✅ 回测更贴近实际（按采样频率生成信号）
- ✅ 数据加载统一管理
- ✅ 支持用户自定义采样频率

